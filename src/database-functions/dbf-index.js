const createBooks = require('./create-books');
const updateBooks = require('./upd-books');
const deleteBooks = require('./del-books');

module.exports = {
    createBooks,
    updateBooks,
    deleteBooks,
}