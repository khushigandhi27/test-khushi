const addBookValidator = require('./add-book-validator');
const updateBookValidator = require('./upd-book-validator');
const deleteBookValidator = require('./delete-book-validator');

module.exports = {
    addBookValidator,
    updateBookValidator,
    deleteBookValidator,
};